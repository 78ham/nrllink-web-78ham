server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Docker 内置 DNS 解析器，用于解析容器名 nrllink
    resolver 127.0.0.11 valid=30s ipv6=off;

    # ---- Gzip compression ----
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_min_length 256;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/javascript
        application/json
        application/xml
        image/svg+xml
        font/woff2;

    # ---- Static assets: long cache ----
    location ~* \.(?:css|js|woff2?|ttf|eot|ico|png|jpe?g|gif|webp|svg|mp3|ogg|wav)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # ---- Backend API: proxy pass ----
    location ~ ^/(user|platform|server|device|group|relay|billing|homepage|log|public|servers|roles|room|operatorlog|weixin) {
        set $backend http://nrllink:9000;
        proxy_pass $backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ---- WebSocket proxy ----
    location ~ ^/ws {
        set $backend http://nrllink:9000;
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ---- Uploaded images: proxy pass to backend ----
    location ^~ /uploads/ {
        set $backend http://nrllink:9000;
        proxy_pass $backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # ---- Fallback: Vue Router (hash mode) ----
    location / {
        try_files $uri $uri/ /index.html;
    }
}
